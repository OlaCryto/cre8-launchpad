// Wallet config — no longer uses RainbowKit
// wagmi config kept for contract reads via viem public client
// Auth is handled by AuthContext (X login + platform-generated wallet)

export { CONTRACTS, TOKEN_CONSTANTS, FEES } from './wagmi';
